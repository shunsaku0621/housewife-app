<div id="favorite-<%= @cook.id %>">
  <% if !current_user.nil? && current_user.favorite?(@cook) %>
    <%= render 'users/unfavorite' %>
  <% else %>
    <%= render 'users/favorite' %>
  <% end %>
</div>